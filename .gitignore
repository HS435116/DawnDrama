# ============ 依赖 ============
node_modules/

# ============ 本地生成数据 (不推送) ============
output/
agnes-settings.json
agnes-data-dir.json

# ============ 密钥与本地数据导出 (绝对不要推送) ============
agnes_*_raw.bin
agnes_*_dump.json
*.bin
dump_*.js
.env
.env.*
*_secrets.json
secrets.json

# ============ 构建产物 (不推送) ============
desktop-dist/
dist/
resources/

# ============ Python 缓存 ============
__pycache__/
*.pyc
*.pyo

# ============ 本地回归测试 (开发者本地保留) ============
tests/

# ============ 系统临时文件 ============
.DS_Store
Thumbs.db

# ============ 工具缓存 ============
.dsh-vision-toolkit/

# ============ 用户个人创作数据 (不推送) ============
剧本/

# 本地开发脚本
分镜视频合成/

# ============ 一次性本地脚本 (含个人剧集内容/本机绝对路径, 不推送) ============
recover_*.py
gen_scenes_*.py

# ============ 本地分析服务 (不随仓库发布) ============
analytics-server/
.dawn-drama-uid.json

# ============ 本地说明文件 (不推送) ============
应用说明.txt
