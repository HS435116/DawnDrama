# 晨曦短剧梦工坊 — 用户使用说明书

**版本：** v2.8.10  
**开发：** 晨曦微光工作室  
**许可：** MIT

---

## 目录

1. [快速入门](#一快速入门)
2. [平台与模型配置](#二平台与模型配置)
3. [批量生成视频](#三批量生成视频)
4. [短剧工坊](#四短剧工坊)
5. [任务扫描](#五任务扫描)
6. [作品库管理](#六作品库管理)
7. [视频合并与字幕烧录](#七视频合并与字幕烧录)
8. [剧本存档](#八剧本存档)
9. [桌面便携版](#九桌面便携版)
10. [常见问题](#十常见问题)

---

## 一、快速入门

### 1.1 两种运行方式

| 方式 | 适用场景 | 特点 |
|------|---------|------|
| **服务器模式**（推荐） | 开发/日常使用 | 浏览器打开，支持代理转发 |
| **桌面便携版** | 无 Node.js 环境 | 免安装，开箱即用 |

### 1.2 服务器模式（命令行启动）

```bash
# 首次运行：安装依赖
npm install

# 启动服务器
npm start

# 浏览器自动打开 http://localhost:3000
```

或双击 `start-server.bat` / `launcher.bat`。

### 1.3 桌面便携版（免安装）

```bash
# 本地打包（只需一次）
npm run desktop:build

# 打开生成的便携版 exe（在 desktop-dist/ 目录）
# 双击即可运行，无需安装 Node.js / Python / FFmpeg
```

便携版内置 Python 3.11 + ffmpeg + SenseVoice ASR 模型（约 300MB），新设备开箱即用。

---

## 二、平台与模型配置

### 2.1 支持的 AI 视频平台

| 平台预设 | 说明 |
|---------|------|
| **自动识别**（推荐） | 根据端点/模型名自动判断 |
| 火山方舟 (Seedance) | `POST /contents/generations/tasks` |
| OpenAI 兼容 | `POST /videos` |
| 完全自定义 | 自定义 JSON 请求体 |

### 2.2 配置步骤

1. 打开应用 → 进入 **⚙️ 模型设置** 页
2. 选择平台预设（推荐"自动识别"）
3. 填写：
   - **API 端点**：如 `https://ark.cn-beijing.volces.com/api/v3`
   - **API 密钥**：从平台控制台获取
   - **模型 ID**：可点击"拉取模型列表"后点选，或手动输入
4. 点击 **保存设置**

### 2.3 推荐模型

| 模型 | 最大时长 | 档位 |
|------|---------|------|
| `doubao-seedance-*` | 60s | 4/8/12/16/20/24/30/60s |
| `agnes-video-2.5-flash` | 12s | 4/8/12s |
| `agnes-video-v2.0` | 5s | 固定 5s |

> 选择模型后，"分镜时长"会自动适配为该模型支持的最大值。

### 2.4 参考图调用方式（文生视频 / 图生视频）

**分镜带参考图时，程序自动走图生视频；没带参考图就自动走文生视频**，不需要手动切换。用哪种图生方式在「模型设置 → 参考图调用方式」里选：

| 选项 | 含义 | 说明 |
|------|------|------|
| **参考图生成**（默认） | 把参考图当"人物/风格参考"一起送进去 | Agnes Video 2.5 系最多 5 张；提示词里可用 `<Picture 1>` 指代第 1 张参考图 |
| **首帧图生视频** | 取第一张参考图当首帧，画面从这张图开始动 | 想让每个分镜都从指定画面起手时用（只用 1 张图） |
| **不使用参考图** | 无论有没有参考图都走文生视频 | 平台不支持图生视频、或想省配额时用 |

各平台实际下发的字段（程序自动选择，无需手填）：

- **Agnes Video 2.5 / 2.5 Flash**：`mode=reference` + `images`（Flash 最多 5 张、2.5 最多 8 张），或 `mode=keyframe` + `first_frame`；Flash 的 `size` 会自动收敛为 `720P`（平台只接受该值）
- **Agnes Video v2.0**：单图走 `image`，多图走 `extra_body.image` + `keyframes`
- **火山方舟 Seedance**：图片放在 `content` 里并带 `role`（首帧 `first_frame` / 尾帧 `last_frame`；Seedance 2.0 系支持 `reference_image` 参考图），最多 9 张
- **其他 OpenAI 兼容聚合站**：各站字段名不统一（`image_urls` / `images` / `first_frame` / `image` / `input_reference` …），程序按候选逐个尝试，**把平台接受的字段记住**，同一端点后续任务直接发对的那个
- **完全自定义 JSON 平台**：模板里可用 `{{image_url}}`（第一张图 URL）、`{{image_urls_json}}`（JSON 数组，直接写 `"images": {{image_urls_json}}`）、`{{mode}}`（`text`/`reference`/`keyframe`）自行拼接

> 参考图取自分镜的"参考图"设置（人物参考图等）；图片需平台可公开访问，并在任务完成前保持有效。

---

## 三、批量生成视频

### 3.1 单条生成

1. 进入 **🎥 批量生成** 页
2. 填写 **项目标题**
3. 填写 **提示词**（描述视频内容）
4. 可上传 **参考图片**（最多 9 张）
5. 点击 **开始批量生成**

### 3.2 批量生成

1. 点击 **添加场景** 或 **批量添加**（每行一个场景描述）
2. 可为每个场景单独设置：提示词、参考图、时长
3. 点击 **开始批量生成**
4. 生成完成后视频自动保存到 `output/video/<标题>/`

### 3.3 任务状态

| 状态 | 说明 |
|------|------|
| ⏳ 排队中 | 已提交，等待平台处理 |
| ▶️ 生成中 | 平台正在渲染 |
| ✅ 完成 | 视频已生成并下载 |
| ❌ 失败 | 请求失败，可点击"重试" |
| ❓ 未知 | 查询超时，可点击"重试轮询" |

---

## 四、短剧工坊

### 4.1 功能概述

短剧工坊用文本 AI 自动编写整集短剧剧本，并一键批量生成全部分镜视频。全程人物面孔/声音/服装描述全剧锁定。

### 4.2 使用流程

**第一步：剧集设定**

填写：
- **剧集名称**：如《示例剧名》
- **类型**：古风奇幻 / 都市 / 科幻...
- **世界观与主线提示词**：核心世界观（400-700 字）
- **每集分镜数**：每集拆分为几个场景（默认 4-10）
- **总集数**：计划生成多少集
- **默认分镜时长**：按所选模型自动适配（Seedance 60s / v2.5 系 12s / v2.0 系 5s）
- **分辨率**：480p / 720p / 1080p
- **画面比例**：9:16（竖屏短剧）/ 16:9 / 1:1

**第二步：AI 生成人物设定卡**

点击 **AI 生成人物设定卡**，AI 自动生成每个角色的：
- 面孔/外形描述
- 声音描述
- 服装描述

**第三步：编写本集剧本**

点击 **AI 写剧本**，AI 会：
1. 读取上集剧情和人物设定卡
2. 编写本集完整分镜剧本
3. 每集末尾保留悬念

**第四步：启动生成**

点击 **启动生成**：
1. 各分镜视频并行提交
2. 生成完成后**自动合并成片 + 烧录中文字幕**
3. 输出 `<剧名>_完整版.mp4`

> 点击 **确认自动生成** 可在生成完成后自动触发视频合并与字幕烧录。

---

## 五、任务扫描

### 5.1 功能说明

点击 **"🔍 任务扫描 (查状态/去重)"** 按钮，程序会：

1. **合并重复**：相同内容的重复任务合并，只查询一次
2. **查询状态**：逐个查询平台任务真实状态
3. **补全取回**：已完成的任务自动下载视频到本地
4. **查重标记**：完成作品查重，标记重复作品供清理

### 5.2 扫描结果弹窗

扫描完成后，根据结果弹出对话框：

**场景 A：有失败/未知任务**
```
📊 扫描完成

❌ 平台返回失败: 1 个
⏳ 仍在生成中: 0 个

可重试任务 (1 个):
  · 示例剧名第4集 (ID: abc12345)

是否立即重试失败的任务？

[🔄 重试失败任务]    [查看作品库]
```

**场景 B：全部正确**
```
✅ 任务扫描完成: 全部 10 个任务状态正确，已补全 3 个
```

### 5.3 操作流程

- 点击 **"🔄 重试失败任务"** → 自动重新监控所有失败/未知任务
- 点击 **"查看作品库"** → 切换到作品库页面查看详情
- 关闭弹窗 → 不做任何操作

---

## 六、作品库管理

### 6.1 查看作品

进入 **🖼️ 作品库** 页，查看所有已生成的视频。

每张卡片用**视频第一帧画面**当封面展示（图片作品直接显示原图）：

- 片段开头是黑场（AI 视频常见的淡入）时，会自动往后取第一张有画面的帧，封面不会是一块黑
- 封面在首次浏览时自动生成并缓存（存放在作品库的 `.posters` 目录），不消耗平台配额；成片重新合并后封面会自动更新
- 取不到画面（文件被移走等）时回退成原来的 🎬/🖼️ 图标，不影响其他功能

### 6.2 任务操作

| 操作 | 说明 |
|------|------|
| 🔄 重试 | 重新提交失败/未知的任务 |
| 📥 重新下载 | 完成但本地下载失败时，重新从平台取回 |
| ⬇️ 下载 | 从本地目录下载视频 |
| 📂 在文件夹中显示 | 打开视频文件所在目录 |
| 🗑️ 删除 | 删除作品记录 |

### 6.3 重复治理

- 相同内容（提示词+时长+参考图）已完成视频直接复用，进度条显示 **"♻️ 复用"**
- 任务扫描完成作品查重，重复作品标记 ♻️ 可勾选后批量删除

---

## 七、视频合并与字幕烧录

### 7.1 自动合并（启动生成时触发）

点击"启动生成"后，全部分镜视频生成完成时自动：
1. 按顺序合并所有分镜视频片段
2. VAD（语音检测）精准切分语音段
3. sherpa-onnx SenseVoice 逐段 ASR 识别
4. 噪声过滤（过滤纯标点、英文单字、短噪声）
5. 长段（>4秒）按时间硬切为多段字幕
6. 烧录微软雅黑字幕到画面底部居中
7. 输出 `<剧名>_完整版.mp4`

**原始分镜片段全部保留，不删除。**

### 7.2 手动合并

进入短剧工坊底部，点击 **📦 手动合并视频**：
1. 弹出文件选择框，多选本地视频文件
2. 可调整合并顺序、剔除片段、自定义成片名称
3. 确认后自动执行合并 + VAD+ASR 字幕烧录

### 7.3 字幕样式

- 字体：微软雅黑
- 字号：20px
- 位置：画面底部居中（MarginV=40）
- 描边：白色描边黑色阴影

---

## 八、剧本存档

### 8.1 功能说明

剧本存档用于跨季续写：第一季做完后保存存档，开新剧时可导入继续，人物面孔/声音/服装锁定描述逐字节继承。

### 8.2 操作流程

**保存存档**
1. 短剧工坊底部点击 **💾 保存存档**
2. 输入存档名称（如"Agent 的未来世界 第1季·至第20集"）
3. 保存内容包括：剧集设定、人物卡、集数历史、悬念

**导入续季**
1. 点击 **📤 启用续季** 或导入导出的 JSON 文件
2. 确认继承内容
3. 季数自动 +1，集数从第 1 集重新开始
4. 标题自动带季标记

**导出/删除**
- **导出**：下载 JSON 文件，备份到硬盘或分享给他人
- **删除**：红色确认后删除存档记录

---

## 九、桌面便携版

### 9.1 打包说明

桌面便携版内置完整运行环境，打包后约 300MB，可在任何 Windows 电脑上直接运行。

### 9.2 本地打包步骤

```bash
# 1. 安装依赖
npm install

# 2. 准备内嵌资源（一次性操作）
#    将以下文件放入 resources/ 目录：
#    resources/
#    ├── python_temp/     # Python 3.11 嵌入版 + sherpa_onnx/soundfile/numpy (约110MB)
#    ├── ffmpeg/          # ffmpeg 7.0.2 (约165MB)
#    └── asr_model/       # SenseVoice ASR 模型 (约229MB)

# 3. 打包
npm run desktop:build
# 产物在 desktop-dist/：
#   晨曦短剧梦工坊-v2.8.10-便携版.exe
#   晨曦短剧梦工坊-安装版-v2.8.10.exe
```

### 9.3 新设备使用

直接复制 `desktop-dist/` 目录到新电脑，双击 exe 即可运行，无需任何安装。

---

## 十、常见问题

**Q: 一直显示"排队中"？**
若个别任务仍显示 ❓未知，说明平台长时间未返回，点击"重试轮询"或到平台控制台核对任务 ID。

**Q: 批量生成时提示 429 / 限流？**
免费平台有速率限制。程序会自动重试并放慢提交；若任务标记为失败，直接点卡片上的"🔄 重试"即可重新提交。也可在"模型设置 → 高级参数 → 提交间隔"调大到 10~30 秒。

**Q: 提示 401/403？**
API 密钥无效，或方舟模型未在控制台开通。

**Q: 提示"余额不足 / 配额耗尽"（平台欠费）？**
程序会自动暂停挂机并拦住后续提交，同时弹出提醒，避免继续无效重试白耗时间。**任务不会丢失**：已完成的分镜、未完成任务连同提交参数与平台任务 ID 都保留在本地。到对应平台充值后，点提醒里的"🔁 我已充值，重新检查"解除阻断，再点未完成任务的"🔄 重试"或"🔍 任务扫描并找回"即可接着跑（平台上已生成完成的会直接取回，不重复消耗配额）。文本模型与视频模型欠费都会这样提醒，弹窗会写明是哪一类模型。

**Q: 想中途结束这次生成，或者换一部剧？**
- **「⏹️ 停止全剧生成 / 停止挂机」= 结束本次运行的全部任务**：中断在途的提交/轮询/下载请求，把还在"排队中/生成中"的任务就地收尾，并清掉本次进度记录（下次打开不会再提示"继续上次任务"）。已保存的片段与成片都保留；平台上已生成完成的任务不会被取消，之后用"🔍 任务扫描并找回"照样能取回。
- **「🗑️ 清除剧本任务」= 删除剧本数据**（设定/人物卡/分镜/剧本历史）并同步清理该剧未完成的任务记录。如果任务还在跑，会先弹对话框让你选择"⏹️ 停止任务并清除"或取消；无论哪种情况，清除后都不保留本次运行记录。已生成完成的作品记录与本地视频文件不会被删除。

**Q: 提示 404？**
检查端点是否正确（方舟需含 `/api/v3` 路径，只填主机名会自动补全）。

**Q: 带参考图的分镜提交失败，报 `image_urls is not an allowed request field`？**
这是旧版本发错字段导致的（平台没有 `image_urls` 这个字段，现在已按各平台官方文档改为 `images` / `first_frame` / `content.role` 等正确写法）。如果你用的是自定义字段的平台：程序会自动尝试并记住平台接受的字段名；仍不行时请在「模型设置 → 参考图调用方式」选择"不使用参考图"（改走文生视频），或改用支持参考图的模型。

**Q: 想看这个版本改了什么、之前几个版本改了什么？**
点顶部"🔄 版本更新"，页面里的「📜 历史版本更新记录」会保留**最近 5 条**版本说明（点标题可展开看全文）。记录来自更新清单 latest.json 的 `history` 字段，联网检查过一次后会缓存到本地，离线也能查看。

**Q: 双击 bat 无反应 / 服务器启动失败？**
确认已安装 Node.js 18+，并运行 `npm install`。

**Q: 便携版打开无反应？**
确认 `resources/` 目录已正确放置（Python/ffmpeg/ASR 模型），或重新执行 `npm run desktop:build`。

**Q: 任务扫描后没有反应？**
扫描完成后会弹出对话框，请查看是否有"重试失败任务"选项，点击即可。

---

## 附录

### 目录结构

```
晨曦短剧梦工坊/
├── index.html               主界面
├── app.js                   前端逻辑（生成流程/短剧工坊/任务扫描/版本更新）
├── api-client.js            多平台 API 适配器
├── server.js                Node 服务器（代理/落盘/作品库/合并接口）
├── merge_videos.py          视频合并 + VAD+ASR 字幕烧录脚本
├── video-poster.js          作品库封面（视频第一帧抽帧 + 缓存）
├── update-downloader.js     应用内更新下载（进度/重定向/超时/取消）
├── electron-process-video.js Electron IPC 处理器
├── electron-main.js         Electron 桌面版主进程
├── styles.css               样式
├── package.json             项目配置（v2.8.10）
├── latest.json              版本更新清单
├── output/                  生成结果（不推送）
│   ├── images/              参考图片
│   └── video/               视频文件
├── resources/               便携模式资源包（不推送）
└── desktop-dist/            桌面版构建产物（不推送）
```

### 关键机制

- **限流自适应**：全局串行排队；触发 429 统一冷却退避（20s→40s→60s→90s→120s）
- **轮询终态保护**：连续 404/网络错误/超 30 分钟标记"未知"
- **重复治理**：同内容已完成视频直接复用
- **模型时长自动适配**：选模型后默认分镜时长自动取最大值
- **欠费阻断**：文本/视频模型返回余额不足或配额耗尽时，自动暂停挂机 + 拦住后续提交 + 弹窗提示充值或换 Key（不再一条条刷成"失败"）；任务与提交参数全部保留，充值后可继续
- **VAD+ASR 字幕**：语音检测 + 噪声过滤 + 长段硬切 + 字幕烧录

---

**版权所有 © 2026 @ 晨曦微光工作室**  
基于 MIT 许可证开源发布
