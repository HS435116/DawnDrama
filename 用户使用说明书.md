# 晨曦短剧梦工坊 — 用户使用说明书

**版本：** v2.8.3  
**开发：** 晨曦微光工作室  
**许可：** MIT

---

## 目录

1. [快速入门](#一快速入门)
2. [平台与模型配置](#二平台与模型配置)
3. [批量生成视频](#三批量生成视频)
4. [短剧工坊](#四短剧工坊)
5. [任务扫描](#五任务扫描)
6. [作品库管理](#六作品库管理)
7. [视频合并与字幕烧录](#七视频合并与字幕烧录)
8. [剧本存档](#八剧本存档)
9. [桌面便携版](#九桌面便携版)
10. [常见问题](#十常见问题)

---

## 一、快速入门

### 1.1 两种运行方式

| 方式 | 适用场景 | 特点 |
|------|---------|------|
| **服务器模式**（推荐） | 开发/日常使用 | 浏览器打开，支持代理转发 |
| **桌面便携版** | 无 Node.js 环境 | 免安装，开箱即用 |

### 1.2 服务器模式（命令行启动）

```bash
# 首次运行：安装依赖
npm install

# 启动服务器
npm start

# 浏览器自动打开 http://localhost:3000
```

或双击 `start-server.bat` / `launcher.bat`。

### 1.3 桌面便携版（免安装）

```bash
# 本地打包（只需一次）
npm run desktop:build

# 打开生成的便携版 exe（在 desktop-dist/ 目录）
# 双击即可运行，无需安装 Node.js / Python / FFmpeg
```

便携版内置 Python 3.11 + ffmpeg + SenseVoice ASR 模型（约 300MB），新设备开箱即用。

---

## 二、平台与模型配置

### 2.1 支持的 AI 视频平台

| 平台预设 | 说明 |
|---------|------|
| **自动识别**（推荐） | 根据端点/模型名自动判断 |
| 火山方舟 (Seedance) | `POST /contents/generations/tasks` |
| OpenAI 兼容 | `POST /videos` |
| 完全自定义 | 自定义 JSON 请求体 |

### 2.2 配置步骤

1. 打开应用 → 进入 **⚙️ 模型设置** 页
2. 选择平台预设（推荐"自动识别"）
3. 填写：
   - **API 端点**：如 `https://ark.cn-beijing.volces.com/api/v3`
   - **API 密钥**：从平台控制台获取
   - **模型 ID**：可点击"拉取模型列表"后点选，或手动输入
4. 点击 **保存设置**

### 2.3 推荐模型

| 模型 | 最大时长 | 档位 |
|------|---------|------|
| `doubao-seedance-*` | 60s | 4/8/12/16/20/24/30/60s |
| `agnes-video-2.5-flash` | 12s | 4/8/12s |
| `agnes-video-v2.0` | 5s | 固定 5s |

> 选择模型后，"分镜时长"会自动适配为该模型支持的最大值。

---

## 三、批量生成视频

### 3.1 单条生成

1. 进入 **🎥 批量生成** 页
2. 填写 **项目标题**
3. 填写 **提示词**（描述视频内容）
4. 可上传 **参考图片**（最多 9 张）
5. 点击 **开始批量生成**

### 3.2 批量生成

1. 点击 **添加场景** 或 **批量添加**（每行一个场景描述）
2. 可为每个场景单独设置：提示词、参考图、时长
3. 点击 **开始批量生成**
4. 生成完成后视频自动保存到 `output/video/<标题>/`

### 3.3 任务状态

| 状态 | 说明 |
|------|------|
| ⏳ 排队中 | 已提交，等待平台处理 |
| ▶️ 生成中 | 平台正在渲染 |
| ✅ 完成 | 视频已生成并下载 |
| ❌ 失败 | 请求失败，可点击"重试" |
| ❓ 未知 | 查询超时，可点击"重试轮询" |

---

## 四、短剧工坊

### 4.1 功能概述

短剧工坊用文本 AI 自动编写整集短剧剧本，并一键批量生成全部分镜视频。全程人物面孔/声音/服装描述全剧锁定。

### 4.2 使用流程

**第一步：剧集设定**

填写：
- **剧集名称**：如《示例剧名》
- **类型**：古风奇幻 / 都市 / 科幻...
- **世界观与主线提示词**：核心世界观（400-700 字）
- **每集分镜数**：每集拆分为几个场景（默认 4-10）
- **总集数**：计划生成多少集
- **默认分镜时长**：按所选模型自动适配（Seedance 60s / v2.5 系 12s / v2.0 系 5s）
- **分辨率**：480p / 720p / 1080p
- **画面比例**：9:16（竖屏短剧）/ 16:9 / 1:1

**第二步：AI 生成人物设定卡**

点击 **AI 生成人物设定卡**，AI 自动生成每个角色的：
- 面孔/外形描述
- 声音描述
- 服装描述

**第三步：编写本集剧本**

点击 **AI 写剧本**，AI 会：
1. 读取上集剧情和人物设定卡
2. 编写本集完整分镜剧本
3. 每集末尾保留悬念

**第四步：启动生成**

点击 **启动生成**：
1. 各分镜视频并行提交
2. 生成完成后**自动合并成片 + 烧录中文字幕**
3. 输出 `<剧名>_完整版.mp4`

> 点击 **确认自动生成** 可在生成完成后自动触发视频合并与字幕烧录。

---

## 五、任务扫描

### 5.1 功能说明

点击 **"🔍 任务扫描 (查状态/去重)"** 按钮，程序会：

1. **合并重复**：相同内容的重复任务合并，只查询一次
2. **查询状态**：逐个查询平台任务真实状态
3. **补全取回**：已完成的任务自动下载视频到本地
4. **查重标记**：完成作品查重，标记重复作品供清理

### 5.2 扫描结果弹窗

扫描完成后，根据结果弹出对话框：

**场景 A：有失败/未知任务**
```
📊 扫描完成

❌ 平台返回失败: 1 个
⏳ 仍在生成中: 0 个

可重试任务 (1 个):
  · 示例剧名第4集 (ID: abc12345)

是否立即重试失败的任务？

[🔄 重试失败任务]    [查看作品库]
```

**场景 B：全部正确**
```
✅ 任务扫描完成: 全部 10 个任务状态正确，已补全 3 个
```

### 5.3 操作流程

- 点击 **"🔄 重试失败任务"** → 自动重新监控所有失败/未知任务
- 点击 **"查看作品库"** → 切换到作品库页面查看详情
- 关闭弹窗 → 不做任何操作

---

## 六、作品库管理

### 6.1 查看作品

进入 **🖼️ 作品库** 页，查看所有已生成的视频。

### 6.2 任务操作

| 操作 | 说明 |
|------|------|
| 🔄 重试 | 重新提交失败/未知的任务 |
| 📥 重新下载 | 完成但本地下载失败时，重新从平台取回 |
| ⬇️ 下载 | 从本地目录下载视频 |
| 📂 在文件夹中显示 | 打开视频文件所在目录 |
| 🗑️ 删除 | 删除作品记录 |

### 6.3 重复治理

- 相同内容（提示词+时长+参考图）已完成视频直接复用，进度条显示 **"♻️ 复用"**
- 任务扫描完成作品查重，重复作品标记 ♻️ 可勾选后批量删除

---

## 七、视频合并与字幕烧录

### 7.1 自动合并（启动生成时触发）

点击"启动生成"后，全部分镜视频生成完成时自动：
1. 按顺序合并所有分镜视频片段
2. VAD（语音检测）精准切分语音段
3. sherpa-onnx SenseVoice 逐段 ASR 识别
4. 噪声过滤（过滤纯标点、英文单字、短噪声）
5. 长段（>4秒）按时间硬切为多段字幕
6. 烧录微软雅黑字幕到画面底部居中
7. 输出 `<剧名>_完整版.mp4`

**原始分镜片段全部保留，不删除。**

### 7.2 手动合并

进入短剧工坊底部，点击 **📦 手动合并视频**：
1. 弹出文件选择框，多选本地视频文件
2. 可调整合并顺序、剔除片段、自定义成片名称
3. 确认后自动执行合并 + VAD+ASR 字幕烧录

### 7.3 字幕样式

- 字体：微软雅黑
- 字号：20px
- 位置：画面底部居中（MarginV=40）
- 描边：白色描边黑色阴影

---

## 八、剧本存档

### 8.1 功能说明

剧本存档用于跨季续写：第一季做完后保存存档，开新剧时可导入继续，人物面孔/声音/服装锁定描述逐字节继承。

### 8.2 操作流程

**保存存档**
1. 短剧工坊底部点击 **💾 保存存档**
2. 输入存档名称（如"Agent 的未来世界 第1季·至第20集"）
3. 保存内容包括：剧集设定、人物卡、集数历史、悬念

**导入续季**
1. 点击 **📤 启用续季** 或导入导出的 JSON 文件
2. 确认继承内容
3. 季数自动 +1，集数从第 1 集重新开始
4. 标题自动带季标记

**导出/删除**
- **导出**：下载 JSON 文件，备份到硬盘或分享给他人
- **删除**：红色确认后删除存档记录

---

## 九、桌面便携版

### 9.1 打包说明

桌面便携版内置完整运行环境，打包后约 300MB，可在任何 Windows 电脑上直接运行。

### 9.2 本地打包步骤

```bash
# 1. 安装依赖
npm install

# 2. 准备内嵌资源（一次性操作）
#    将以下文件放入 resources/ 目录：
#    resources/
#    ├── python_temp/     # Python 3.11 嵌入版 + sherpa_onnx/soundfile/numpy (约110MB)
#    ├── ffmpeg/          # ffmpeg 7.0.2 (约165MB)
#    └── asr_model/       # SenseVoice ASR 模型 (约229MB)

# 3. 打包
npm run desktop:build
# 产物在 desktop-dist/：
#   晨曦短剧梦工坊-v2.8.3-便携版.exe
#   晨曦短剧梦工坊-v2.8.3-安装版.exe
```

### 9.3 新设备使用

直接复制 `desktop-dist/` 目录到新电脑，双击 exe 即可运行，无需任何安装。

---

## 十、常见问题

**Q: 一直显示"排队中"？**
若个别任务仍显示 ❓未知，说明平台长时间未返回，点击"重试轮询"或到平台控制台核对任务 ID。

**Q: 批量生成时提示 429 / 限流？**
免费平台有速率限制。程序会自动重试并放慢提交；若任务标记为失败，直接点卡片上的"🔄 重试"即可重新提交。也可在"模型设置 → 高级参数 → 提交间隔"调大到 10~30 秒。

**Q: 提示 401/403？**
API 密钥无效，或方舟模型未在控制台开通。

**Q: 提示 404？**
检查端点是否正确（方舟需含 `/api/v3` 路径，只填主机名会自动补全）。

**Q: 双击 bat 无反应 / 服务器启动失败？**
确认已安装 Node.js 18+，并运行 `npm install`。

**Q: 便携版打开无反应？**
确认 `resources/` 目录已正确放置（Python/ffmpeg/ASR 模型），或重新执行 `npm run desktop:build`。

**Q: 任务扫描后没有反应？**
扫描完成后会弹出对话框，请查看是否有"重试失败任务"选项，点击即可。

---

## 附录

### 目录结构

```
晨曦短剧梦工坊/
├── index.html               主界面
├── app.js                   前端逻辑（生成流程/短剧工坊/任务扫描/版本更新）
├── api-client.js            多平台 API 适配器
├── server.js                Node 服务器（代理/落盘/作品库/合并接口）
├── merge_videos.py          视频合并 + VAD+ASR 字幕烧录脚本
├── electron-process-video.js Electron IPC 处理器
├── electron-main.js         Electron 桌面版主进程
├── styles.css               样式
├── package.json             项目配置（v2.8.3）
├── latest.json              版本更新清单
├── output/                  生成结果（不推送）
│   ├── images/              参考图片
│   └── video/               视频文件
├── resources/               便携模式资源包（不推送）
└── desktop-dist/            桌面版构建产物（不推送）
```

### 关键机制

- **限流自适应**：全局串行排队；触发 429 统一冷却退避（20s→40s→60s→90s→120s）
- **轮询终态保护**：连续 404/网络错误/超 30 分钟标记"未知"
- **重复治理**：同内容已完成视频直接复用
- **模型时长自动适配**：选模型后默认分镜时长自动取最大值
- **VAD+ASR 字幕**：语音检测 + 噪声过滤 + 长段硬切 + 字幕烧录

---

**版权所有 © 2026 @ 晨曦微光工作室**  
基于 MIT 许可证开源发布
